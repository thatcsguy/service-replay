{
  "name": "rama-replay",
  "version": "1.0.0",
  "description": "CLI tool to replay GraphQL queries and compare local vs production responses",
  "main": "dist/index.js",
  "bin": {
    "rama-replay": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "type": "module",
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
